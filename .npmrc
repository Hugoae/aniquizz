auto-install-peers=true
strict-peer-dependencies=false
# Run lifecycle scripts in Git Bash instead of cmd.exe (avoids the Windows
# "Terminate batch job (O/N)" prompt that blocks Ctrl+C on `pnpm dev`).
script-shell="C:\\Program Files\\Git\\bin\\bash.exe"
