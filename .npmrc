auto-install-peers=true
strict-peer-dependencies=false
# NOTE: Do NOT set a Windows-specific `script-shell` here — it breaks Linux CI
# (Vercel/Render) because the shell path doesn't exist and every lifecycle
# script fails to spawn. Windows devs who want Git Bash for lifecycle scripts
# should set `script-shell` in their user-level ~/.npmrc instead.
